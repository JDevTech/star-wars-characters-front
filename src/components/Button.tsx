import styled from "styled-components";

const Button = styled.button`
  border: none;
  padding: 10px;
  cursor: pointer;
  margin: 0px 10px;
  font-weight: 500;
  border-radius: 10px;
`;

export default Button;
